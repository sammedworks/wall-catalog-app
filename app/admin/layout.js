export const metadata = {
  title: 'Admin Dashboard - Wall Catalog',
  description: 'Manage your wall catalog products, enquiries, and quotations',
};

export default function AdminLayout({ children }) {
  return children;
}